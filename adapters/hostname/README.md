# hostname

Shapes adapter for `hostname` — show system hostname.

## Operations

| ID | Command | Risk | Description |
|----|---------|------|-------------|
| `hostname.info` | `hostname` | low | Show the system hostname |

## Examples

```bash
apes explain -- hostname
```
